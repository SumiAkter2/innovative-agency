import React from 'react';

const Button = () => {
    return (
        <div>
            <button class="btn btn-active bg-gradient-to-r from-violet-400  font-bold text-xl">Get Started</button>
        </div>
    );
};

export default Button;